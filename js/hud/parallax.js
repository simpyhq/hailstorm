/* parallax.js — rAF + lerp 3D parallax engine. Implemented in Step 3.
   Stub keeps main.js's call harmless until then. */
(function (J) {
  'use strict';
  J.parallax = { init() {}, register() {} };
})(window.JARVIS = window.JARVIS || {});
